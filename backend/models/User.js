// User model — Demo
// Example shape of a customer/user.

const UserSchema = {
  id: "Number — unique id",
  name: "String — full name (e.g. Ahmad)",
  phone: "String — Saudi phone number",
  role: "String — customer / vendor / admin",
  city: "String — e.g. Riyadh, Jeddah",
};

module.exports = UserSchema;
