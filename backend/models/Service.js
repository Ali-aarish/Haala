// Service model — Demo
// Example shape of a service listing.

const ServiceSchema = {
  id: "Number — unique id",
  title: "String — e.g. Home Cleaning, AC Repair",
  price: "Number — price in SAR",
  vendorId: "Number — provider offering the service",
  category: "String — Cleaning / AC / Plumbing",
};

module.exports = ServiceSchema;
